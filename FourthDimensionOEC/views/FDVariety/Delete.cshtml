@model FourthDimensionOEC.Models.Variety
@using Microsoft.AspNetCore.Http

@{
    ViewData["Title"] = $"Delete Variety for crop {Context.Session.GetString("SessVarCropName")}";
}

<h2>@ViewData["Title"]</h2>

<h3 style="color:darkred;">Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="VarietyId" />
        <input type="submit" value="Delete" class="btn-lg btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
